" Since currently no any required features, don't actually load anything here
execute 'source ' . stdpath('config') . '/vimrc/config/todo.vim'
if v:true | finish | endif

execute 'source ' . stdpath('config') . '/vimrc/config/init.vim'
